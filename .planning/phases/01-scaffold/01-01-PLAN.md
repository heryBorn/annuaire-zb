---
phase: 01-scaffold
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - annuaire-zb-react/package.json
  - annuaire-zb-react/public/images/logo_zb_trans.png
  - annuaire-zb-react/.env
autonomous: true
requirements:
  - SCAF-01
  - SCAF-06
  - SCAF-07

must_haves:
  truths:
    - "A CRA project directory annuaire-zb-react/ exists inside the project root with a working node_modules and package.json"
    - "npm start inside annuaire-zb-react/ launches a React app with no compile errors"
    - "annuaire-zb-react/.env contains REACT_APP_SHEET_API_URL with the Google Apps Script URL"
    - "annuaire-zb-react/public/images/logo_zb_trans.png exists and is the correct logo file"
  artifacts:
    - path: "annuaire-zb-react/package.json"
      provides: "CRA project manifest with react-scripts 5"
      contains: "react-scripts"
    - path: "annuaire-zb-react/.env"
      provides: "Environment variable with REACT_APP_ prefix"
      contains: "REACT_APP_SHEET_API_URL"
    - path: "annuaire-zb-react/public/images/logo_zb_trans.png"
      provides: "Logo asset in CRA public directory"
  key_links:
    - from: "annuaire-zb-react/.env"
      to: "CRA webpack DefinePlugin"
      via: "REACT_APP_ prefix"
      pattern: "REACT_APP_SHEET_API_URL"
    - from: "assets/images/logo_zb_trans.png"
      to: "annuaire-zb-react/public/images/logo_zb_trans.png"
      via: "cp command"
      pattern: "logo_zb_trans.png"
---

<objective>
Bootstrap the Create React App project inside the existing repository, copy the logo asset to the CRA public directory, and create the correctly-prefixed .env file.

Purpose: Establish the CRA foundation that all subsequent plans build on. Without this, no other configuration task can proceed.
Output: annuaire-zb-react/ directory with a running React app, correct logo asset, and correct env file.
</objective>

<execution_context>
@C:/Users/eklez/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eklez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap CRA project</name>
  <files>annuaire-zb-react/package.json</files>
  <action>
    From the project root D:/Workspace/Php/www/annuaire-zb/, run:

    ```bash
    npx create-react-app annuaire-zb-react
    ```

    This creates annuaire-zb-react/ as a subdirectory of the existing project root. The existing index.html and inscription.html at the root are NOT touched — CRA creates its own isolated directory.

    After CRA finishes, verify the generated src/index.js uses ReactDOM.createRoot (CRA 5 default). If it does not, replace it with:

    ```jsx
    import React from 'react';
    import ReactDOM from 'react-dom/client';
    import './index.css';
    import App from './App';

    const root = ReactDOM.createRoot(document.getElementById('root'));
    root.render(<React.StrictMode><App /></React.StrictMode>);
    ```

    Do NOT eject. Do NOT modify any other generated files in this task.
  </action>
  <verify>
    <automated>cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react && node -e "const p=require('./package.json'); if(!p.dependencies['react-scripts']) process.exit(1); console.log('CRA OK, react-scripts:', p.dependencies['react-scripts'])"</automated>
  </verify>
  <done>annuaire-zb-react/package.json exists and lists react-scripts as a dependency</done>
</task>

<task type="auto">
  <name>Task 2: Copy logo asset to CRA public directory</name>
  <files>annuaire-zb-react/public/images/logo_zb_trans.png</files>
  <action>
    Create the images subdirectory inside CRA's public folder and copy the logo:

    ```bash
    mkdir -p D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/public/images
    cp D:/Workspace/Php/www/annuaire-zb/assets/images/logo_zb_trans.png \
       D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/public/images/logo_zb_trans.png
    ```

    In CRA, files in public/ are served at the root path. The logo will be accessible as /images/logo_zb_trans.png in browser requests. Later components reference it as `<img src="/images/logo_zb_trans.png" alt="Logo ZB" />` — do NOT import it as a JS module (the public/ path approach is consistent with how Phase 2 will use it).
  </action>
  <verify>
    <automated>test -f D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/public/images/logo_zb_trans.png && echo "Logo OK" || echo "MISSING"</automated>
  </verify>
  <done>annuaire-zb-react/public/images/logo_zb_trans.png exists</done>
</task>

<task type="auto">
  <name>Task 3: Create .env with REACT_APP_ prefix</name>
  <files>annuaire-zb-react/.env</files>
  <action>
    Create a new .env file inside annuaire-zb-react/ (NOT at the project root — that file stays untouched):

    ```
    REACT_APP_SHEET_API_URL=https://script.google.com/macros/s/AKfycbztDy9LxShVHeRJpdvsEUa84eVh1kiqaDra6V_k0XbxoiiV3Pcm3XIMx2hbsZE4f_BX/exec
    ```

    Critical rules:
    - The key MUST start with REACT_APP_ — CRA's webpack DefinePlugin only exposes variables with this prefix. The parent directory .env uses SHEET_API_URL (no prefix) which CRA silently ignores.
    - The URL value is copied verbatim from D:/Workspace/Php/www/annuaire-zb/.env.
    - After creating this file, do NOT start the dev server yet — the dev server is started in the verify step below.
    - Add annuaire-zb-react/.env to annuaire-zb-react/.gitignore if not already present (CRA generates .gitignore with .env already excluded).
  </action>
  <verify>
    <automated>cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react && node -e "require('dotenv').config(); const val=process.env.REACT_APP_SHEET_API_URL; if(!val||!val.startsWith('https://script.google.com')) process.exit(1); console.log('ENV OK:', val.substring(0,60)+'...')" 2>/dev/null || (grep -q "REACT_APP_SHEET_API_URL=https" .env && echo "ENV file OK (dotenv not installed yet — checked file directly)")</automated>
  </verify>
  <done>annuaire-zb-react/.env contains REACT_APP_SHEET_API_URL=https://script.google.com/...</done>
</task>

</tasks>

<verification>
After all three tasks complete, run a smoke test from inside annuaire-zb-react/:

```bash
cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react
npm start
```

Expected: Browser opens at http://localhost:3000 showing the default CRA React logo page with no red error overlay and no console errors. (Tailwind and routing are configured in the next plans — the default CRA page is correct here.)

Stop the dev server after confirming startup (Ctrl+C).
</verification>

<success_criteria>
- annuaire-zb-react/ directory exists with node_modules/ and package.json listing react-scripts
- annuaire-zb-react/public/images/logo_zb_trans.png exists (copied from assets/images/)
- annuaire-zb-react/.env contains REACT_APP_SHEET_API_URL with the Google Apps Script URL
- npm start in annuaire-zb-react/ compiles and serves the app with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-01-SUMMARY.md`
</output>
