---
phase: 01-scaffold
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - annuaire-zb-react/package.json
  - annuaire-zb-react/src/App.js
  - annuaire-zb-react/src/pages/DirectoryPage.jsx
  - annuaire-zb-react/src/pages/InscriptionPage.jsx
autonomous: true
requirements:
  - SCAF-02
  - SCAF-05

must_haves:
  truths:
    - "Visiting / renders DirectoryPage stub without a full page reload"
    - "Visiting /inscription renders InscriptionPage stub without a full page reload"
    - "A FontAwesome icon renders visibly in App.js without importing any CSS file"
    - "process.env.REACT_APP_SHEET_API_URL is visible as the correct URL string in the browser (not undefined)"
  artifacts:
    - path: "annuaire-zb-react/src/App.js"
      provides: "BrowserRouter + Routes setup with two routes"
      contains: "BrowserRouter"
    - path: "annuaire-zb-react/src/pages/DirectoryPage.jsx"
      provides: "Stub component for the directory route"
      exports: ["DirectoryPage"]
    - path: "annuaire-zb-react/src/pages/InscriptionPage.jsx"
      provides: "Stub component for the registration route"
      exports: ["InscriptionPage"]
  key_links:
    - from: "annuaire-zb-react/src/App.js"
      to: "src/pages/DirectoryPage.jsx"
      via: "Route path=/ element={<DirectoryPage />}"
      pattern: "path=\"/\""
    - from: "annuaire-zb-react/src/App.js"
      to: "src/pages/InscriptionPage.jsx"
      via: "Route path=/inscription element={<InscriptionPage />}"
      pattern: "path=\"/inscription\""
---

<objective>
Install React Router v6 and FontAwesome SVG packages, wire the two routes in App.js, create page stub components, and add a verification element confirming FontAwesome and env var resolution.

Purpose: Establishes the routing skeleton and icon library so Phase 2 can build the header/nav and page components directly into the correct slots.
Output: Two navigable route stubs, FontAwesome installed and rendering, env var confirmed accessible.
</objective>

<execution_context>
@C:/Users/eklez/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eklez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-scaffold/01-RESEARCH.md
@.planning/phases/01-scaffold/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install React Router v6 and FontAwesome SVG packages</name>
  <files>annuaire-zb-react/package.json</files>
  <action>
    From inside annuaire-zb-react/:

    ```bash
    cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react
    npm install react-router-dom@6
    npm install @fortawesome/fontawesome-svg-core @fortawesome/free-solid-svg-icons @fortawesome/free-brands-svg-icons @fortawesome/react-fontawesome
    ```

    Install all four FontAwesome packages in a single command. Do NOT install the CSS-based `font-awesome` package — it injects global styles that conflict with Tailwind (see RESEARCH.md anti-patterns). The SVG approach is tree-shakeable and requires no CSS imports.

    After installation, confirm the packages appear in package.json dependencies.
  </action>
  <verify>
    <automated>cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react && node -e "const p=require('./package.json'); const deps=p.dependencies; if(!deps['react-router-dom']||!deps['@fortawesome/react-fontawesome']||!deps['@fortawesome/fontawesome-svg-core']) process.exit(1); console.log('Router:', deps['react-router-dom'], '| FA:', deps['@fortawesome/react-fontawesome'])"</automated>
  </verify>
  <done>package.json lists react-router-dom@6.x and all four @fortawesome/* packages in dependencies</done>
</task>

<task type="auto">
  <name>Task 2: Wire routes in App.js and create page stubs</name>
  <files>
    annuaire-zb-react/src/App.js
    annuaire-zb-react/src/pages/DirectoryPage.jsx
    annuaire-zb-react/src/pages/InscriptionPage.jsx
  </files>
  <action>
    Create the src/pages/ directory and the two stub page files.

    **annuaire-zb-react/src/pages/DirectoryPage.jsx:**
    ```jsx
    function DirectoryPage() {
      return (
        <main className="p-8">
          <h1 className="font-serif text-3xl text-soil">Annuaire ZB</h1>
          <p className="font-sans text-muted mt-2">Page annuaire — à construire en Phase 3</p>
        </main>
      );
    }

    export default DirectoryPage;
    ```

    **annuaire-zb-react/src/pages/InscriptionPage.jsx:**
    ```jsx
    function InscriptionPage() {
      return (
        <main className="p-8">
          <h1 className="font-serif text-3xl text-soil">Inscription</h1>
          <p className="font-sans text-muted mt-2">Formulaire d'inscription — à construire en Phase 5</p>
        </main>
      );
    }

    export default InscriptionPage;
    ```

    **annuaire-zb-react/src/App.js** — replace the entire file:
    ```jsx
    import { BrowserRouter, Routes, Route } from 'react-router-dom';
    import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
    import { faLeaf } from '@fortawesome/free-solid-svg-icons';
    import DirectoryPage from './pages/DirectoryPage';
    import InscriptionPage from './pages/InscriptionPage';

    function App() {
      return (
        <BrowserRouter>
          {/* Verification banner — remove after Phase 1 checkpoint */}
          <div className="bg-soil text-cream text-xs px-3 py-1 flex items-center gap-2">
            <FontAwesomeIcon icon={faLeaf} />
            <span>FA + Tailwind OK</span>
            <span className="ml-auto opacity-60">{process.env.REACT_APP_SHEET_API_URL ? 'ENV OK' : 'ENV MISSING'}</span>
          </div>
          <Routes>
            <Route path="/" element={<DirectoryPage />} />
            <Route path="/inscription" element={<InscriptionPage />} />
          </Routes>
        </BrowserRouter>
      );
    }

    export default App;
    ```

    This verification banner confirms: FontAwesome SVG renders (faLeaf icon), Tailwind custom colors apply (bg-soil, text-cream), and env var resolves (shows "ENV OK" or "ENV MISSING"). The banner will be replaced by the real header in Phase 2.

    Use React Router v6 syntax only:
    - `<Routes>` not `<Switch>` (v5 syntax — will throw a compile error)
    - `element={<Component />}` not `component={Component}`
    - No `exact` prop needed — v6 matches exactly by default
  </action>
  <verify>
    <automated>cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react && grep -q "BrowserRouter" src/App.js && grep -q "Routes" src/App.js && grep -q "/inscription" src/App.js && test -f src/pages/DirectoryPage.jsx && test -f src/pages/InscriptionPage.jsx && echo "Routing structure OK" || echo "FAIL"</automated>
  </verify>
  <done>App.js uses BrowserRouter+Routes with two Route entries; both page stub files exist in src/pages/</done>
</task>

</tasks>

<verification>
Start the dev server (restart required because .env was created in Plan 01):

```bash
cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react
npm start
```

Verify all five Phase 1 success criteria:

1. No console errors and no missing stylesheet warnings on startup
2. The verification banner shows a dark-brown background (bg-soil = #2C1A0E) — confirms Tailwind custom color renders
3. The banner shows "ENV OK" (not "ENV MISSING") — confirms REACT_APP_SHEET_API_URL resolves
4. A leaf icon (FontAwesome faLeaf) is visible in the banner without any CSS import — confirms FontAwesome SVG works
5. Open DevTools Network tab — fonts.googleapis.com returns 200 (Google Fonts loaded, no 404)

Also navigate manually:
- http://localhost:3000 — shows "Annuaire ZB" heading in serif font on cream background
- http://localhost:3000/inscription — shows "Inscription" heading without full page reload

If "ENV MISSING" appears: stop npm start, confirm annuaire-zb-react/.env has REACT_APP_SHEET_API_URL= (not SHEET_API_URL=), then restart npm start.
</verification>

<success_criteria>
- react-router-dom@6 and all four @fortawesome/* packages are in package.json
- App.js uses BrowserRouter + Routes + Route with v6 syntax (no Switch, no component prop)
- Navigating to / and /inscription renders the correct stub page
- FontAwesome faLeaf icon renders visibly in the browser without a CSS import
- process.env.REACT_APP_SHEET_API_URL resolves to the Google Apps Script URL in the browser ("ENV OK" shown)
- All five Phase 1 success criteria from ROADMAP.md are met
</success_criteria>

<output>
After completion, create `.planning/phases/01-scaffold/01-03-SUMMARY.md`
</output>
