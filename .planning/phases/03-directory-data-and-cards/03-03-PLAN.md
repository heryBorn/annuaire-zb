---
phase: 03-directory-data-and-cards
plan: "03"
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - annuaire-zb-react/src/pages/DirectoryPage.jsx
autonomous: false
requirements:
  - DIR-01
  - DIR-02
  - DIR-03
  - DIR-04

must_haves:
  truths:
    - "On page load, 8 skeleton cards appear immediately while fetch is in flight"
    - "After fetch completes, member cards fill the responsive 4-column grid (photo, name, title, company, availability badge)"
    - "Stats bar shows total member count, domain count, and available count derived from fetched data"
    - "If fetch returns zero members, an empty state message replaces the card grid"
    - "If fetch rejects with a network error, an error message replaces the skeleton grid"
  artifacts:
    - path: "annuaire-zb-react/src/pages/DirectoryPage.jsx"
      provides: "Fully functional directory page"
      exports: ["DirectoryPage (default)"]
  key_links:
    - from: "DirectoryPage.jsx"
      to: "useMemberFetch"
      via: "import and destructure { members, loading, error }"
      pattern: "useMemberFetch"
    - from: "DirectoryPage.jsx"
      to: "stats (total, domains, avail)"
      via: "useMemo derived from members array"
      pattern: "useMemo"
    - from: "DirectoryPage.jsx"
      to: "MemberCard / SkeletonCard"
      via: "conditional render on loading state"
      pattern: "loading.*SkeletonCard|SkeletonCard.*loading"
---

<objective>
Extend DirectoryPage to orchestrate the full directory experience: fetch members via useMemberFetch, derive stats with useMemo, and render the card grid with loading/empty/error states.

Purpose: This is the final wiring plan — it connects the hook (Plan 01) and card components (Plan 02) into a working page. Ends with a human verify checkpoint.
Output: Fully functional DirectoryPage.jsx. Human confirms the page works correctly in the browser.
</objective>

<execution_context>
@C:/Users/eklez/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/eklez/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md

<!-- Interfaces from prior plans:

From annuaire-zb-react/src/hooks/useMemberFetch.js:
  export function useMemberFetch() { ... }
  Returns: { members: Array, loading: boolean, error: string|null }
  members[] shape: { nom, prenom, metier, entreprise, disponibilite, domaine, photo_url, email, ... }

From annuaire-zb-react/src/components/MemberCard.jsx:
  export default MemberCard;
  Props: { member: Object }
  Usage: <MemberCard key={m.email || m.nom} member={m} />

From annuaire-zb-react/src/components/SkeletonCard.jsx:
  export default SkeletonCard;
  Props: none
  Usage: Array.from({ length: 8 }).map((_, i) => <SkeletonCard key={i} />)

Stats derivation (confirmed from existing index.html buildStats()):
  total   = members.length
  domains = new Set(members.map(m => m.domaine).filter(Boolean)).size
  avail   = members.filter(m => m.disponibilite && m.disponibilite.includes('Disponible')).length
  Note: 'Disponible' includes both 'Disponible' and 'Partiellement disponible' — existing app counts both for this stat

Current DirectoryPage.jsx (stub to replace):
  function DirectoryPage() {
    return (
      <main className="p-8">
        <h1 className="font-serif text-3xl text-soil">Annuaire ZB</h1>
        <p className="font-sans text-muted mt-2">Page annuaire — à construire en Phase 3</p>
      </main>
    );
  }
-->

<!-- App layout:
  Header fixed h-14, content wrapped in pt-14 in App.js
  Use max-w-7xl mx-auto px-6 py-8 for page container
  Grid: grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6
-->
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build DirectoryPage with fetch, stats, and card grid</name>
  <files>annuaire-zb-react/src/pages/DirectoryPage.jsx</files>
  <action>
Replace the stub DirectoryPage.jsx entirely with the full implementation:

```jsx
import { useMemo } from 'react';
import { useMemberFetch } from '../hooks/useMemberFetch';
import MemberCard from '../components/MemberCard';
import SkeletonCard from '../components/SkeletonCard';

function deriveStats(members) {
  const total   = members.length;
  const domains = new Set(members.map(m => m.domaine).filter(Boolean)).size;
  const avail   = members.filter(m =>
    m.disponibilite && m.disponibilite.includes('Disponible')
  ).length;
  return { total, domains, avail };
}

function StatCard({ value, label }) {
  return (
    <div className="bg-white rounded-xl shadow-sm px-6 py-4 text-center">
      <p className="font-serif text-3xl font-bold text-soil">{value}</p>
      <p className="font-sans text-sm text-muted mt-1">{label}</p>
    </div>
  );
}

function EmptyState() {
  return (
    <div className="col-span-full flex flex-col items-center justify-center py-20 text-center">
      <p className="font-serif text-2xl text-muted">Aucun membre trouvé</p>
      <p className="font-sans text-sm text-muted mt-2">L'annuaire est vide pour le moment.</p>
    </div>
  );
}

function ErrorState({ message }) {
  return (
    <div className="col-span-full flex flex-col items-center justify-center py-20 text-center">
      <p className="font-serif text-xl text-terracotta">Impossible de charger l'annuaire</p>
      <p className="font-sans text-sm text-muted mt-2">{message}</p>
    </div>
  );
}

function DirectoryPage() {
  const { members, loading, error } = useMemberFetch();
  const stats = useMemo(() => deriveStats(members), [members]);

  return (
    <main className="px-6 py-8 max-w-7xl mx-auto">
      {/* Stats bar — shows 0s while loading, live values after fetch */}
      <div className="grid grid-cols-3 gap-4 mb-8">
        <StatCard value={stats.total}   label="membres" />
        <StatCard value={stats.domains} label="domaines" />
        <StatCard value={stats.avail}   label="disponibles" />
      </div>

      {/* Card grid */}
      <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-6">
        {error
          ? <ErrorState message={error} />
          : loading
            ? Array.from({ length: 8 }).map((_, i) => <SkeletonCard key={i} />)
            : members.length === 0
              ? <EmptyState />
              : members.map(m => <MemberCard key={m.email || m.nom} member={m} />)
        }
      </div>
    </main>
  );
}

export default DirectoryPage;
```

Critical rules:
- Use `useMemo` for stats — NEVER store total/domains/avail as separate useState. Separate state will go stale.
- Error state takes priority over loading state in the render branch (network failure should be visible, not a stuck spinner).
- Show exactly 8 skeleton cards — fills 2 full rows at lg:grid-cols-4 desktop, minimizes layout shift on data arrival.
- Grid responsive breakpoints: `grid-cols-1 sm:grid-cols-2 lg:grid-cols-4` (locked decision from CONTEXT.md: 4 columns on desktop).
- Stats show 0s during loading (not "—" or undefined) — useMemo returns zeroes immediately on empty array.
- `col-span-full` on EmptyState and ErrorState spans all grid columns so the message is centered in the full grid width.
  </action>
  <verify>
    <automated>test -f D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/src/pages/DirectoryPage.jsx && grep -q "useMemberFetch" D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/src/pages/DirectoryPage.jsx && grep -q "useMemo" D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/src/pages/DirectoryPage.jsx && grep -q "SkeletonCard" D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/src/pages/DirectoryPage.jsx && grep -q "MemberCard" D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react/src/pages/DirectoryPage.jsx && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>DirectoryPage.jsx imports and uses useMemberFetch, useMemo, MemberCard, SkeletonCard. Stats derived via useMemo. Grid uses lg:grid-cols-4.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify directory page in browser</name>
  <action>
Run the dev server and open the directory page for human verification:

```bash
cd annuaire-zb-react && npm start
```

Then open http://localhost:3000/ in the browser.
  </action>
  <verify>
    <automated>cd D:/Workspace/Php/www/annuaire-zb/annuaire-zb-react && npx react-scripts build 2>&1 | tail -3</automated>
  </verify>
  <done>Human confirms all 7 checklist items below are working correctly and types "approved".</done>
  <what-built>
    The full directory page: fetch from Google Apps Script, skeleton loading cards, responsive member card grid, stats bar, empty state.
  </what-built>
  <how-to-verify>
    1. On page load — confirm 8 skeleton cards with pulse animation appear in a 4-column grid before real data loads
    2. After data loads — confirm member cards appear with photo (or initials), name, title, company, and a colored availability badge
    3. Stats bar — confirm 3 stat counters appear with real numbers (total members, domains, available count)
    4. Hover a card — confirm shadow strengthens and card scales up slightly (the "lift" effect)
    5. Responsive test — narrow the browser to mobile width (less than 640px): cards should stack to 1 column
    6. Network error test (optional) — disable network in DevTools, reload: confirm error message replaces skeleton grid
    7. Check browser console — no React warnings, no "undefined?action=getMembers" fetch errors, no missing key warnings
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any issues to fix</resume-signal>
</task>

</tasks>

<verification>
Build passes before human verify:

```bash
cd annuaire-zb-react && npx react-scripts build 2>&1 | tail -5
```

Human verifies the full directory experience in browser.
</verification>

<success_criteria>
- Skeleton cards visible on page load (8 cards, pulse animation, 4-column grid)
- Member cards rendered with all 5 fields (photo/initials, name, title, company, availability badge)
- Stats bar shows live counts derived from fetched data
- Hover lift effect: shadow + scale via CSS transition
- Responsive: 1 col mobile, 2 col tablet, 4 col desktop
- Empty state renders when zero members returned
- No console errors or React warnings
- Human approves at verification checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/03-directory-data-and-cards/03-03-SUMMARY.md`
</output>
